import type { ResolvableValue } from '@d-fischer/shared-utils';
import { ApiClient } from 'twitch';
import type { AuthProvider } from 'twitch-auth';
import { ChatClient } from 'twitch-chat-client';
import type { BotCommand } from './BotCommand';
export interface BotConfig {
    auth?: string | AuthProvider;
    client?: ApiClient;
    debug?: boolean;
    channel?: string;
    channels?: ResolvableValue<string[]>;
    commands?: BotCommand[];
    prefix?: string;
}
export declare class Bot {
    readonly api: ApiClient;
    readonly chat: ChatClient;
    private readonly _commands;
    private readonly _prefix;
    static create(config: BotConfig): Promise<Bot>;
    constructor(api: ApiClient, { channel, channels, debug, commands, prefix }: BotConfig);
    private static _createApiClientForConfig;
    private findMatch;
}
