import type { HelixResponse, HelixSubscriptionEventData } from 'twitch';
import { HelixSubscriptionEvent } from 'twitch';
import type { WebHookListener } from '../WebHookListener';
import { Subscription } from './Subscription';
/**
 * @private
 */
export declare class SubscriptionEventSubscription extends Subscription<HelixSubscriptionEvent> {
    private readonly _broadcasterId;
    constructor(handler: (data: HelixSubscriptionEvent) => void, client: WebHookListener, validityInSeconds: number | undefined, _broadcasterId: string);
    get id(): string;
    protected transformData(response: HelixResponse<HelixSubscriptionEventData>): HelixSubscriptionEvent;
    protected _subscribe(): Promise<void>;
    protected _unsubscribe(): Promise<void>;
}
