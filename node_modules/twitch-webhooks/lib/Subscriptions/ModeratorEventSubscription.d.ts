import type { HelixModeratorEventData, HelixResponse } from 'twitch';
import { HelixModeratorEvent } from 'twitch';
import type { WebHookListener } from '../WebHookListener';
import { Subscription } from './Subscription';
/**
 * @private
 */
export declare class ModeratorEventSubscription extends Subscription<HelixModeratorEvent> {
    private readonly _broadcasterId;
    private readonly _userId?;
    constructor(handler: (data: HelixModeratorEvent) => void, client: WebHookListener, validityInSeconds: number | undefined, _broadcasterId: string, _userId?: string | undefined);
    get id(): string;
    protected transformData(response: HelixResponse<HelixModeratorEventData>): HelixModeratorEvent;
    protected _subscribe(): Promise<void>;
    protected _unsubscribe(): Promise<void>;
}
